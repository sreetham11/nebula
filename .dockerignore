venv/
**/__pycache__/
**/*.pyc
.DS_Store
.git/
.gitignore

# Regenerated at build time by data_gen + the pipeline run, so shipping the
# local copies would only make the build context bigger.
data/
validation_outputs/

# dashboard/ and models/ ARE needed now: the API serves the page itself, and
# models/real_* are Colab-trained artifacts the build cannot regenerate.

*.md
